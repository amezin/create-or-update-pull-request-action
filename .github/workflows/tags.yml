name: tags

on:
  push:
    tags:
      - v[0-9]+.[0-9]+.[0-9]+

jobs:
  tag:
    uses: amezin/js-actions-common/.github/workflows/shared-tags.yml@c282d8b68b27a0ebd4667d0168c30be058a4d640 # v1.0.5
    permissions:
      contents: write
