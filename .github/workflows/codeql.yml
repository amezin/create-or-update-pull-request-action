name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '18 11 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@933656d56f84eb22c3bdf9d092f793dc7d5c5daa # v2.0.1
    permissions:
      contents: read
      security-events: write
