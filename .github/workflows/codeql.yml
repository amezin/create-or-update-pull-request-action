name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '18 11 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@961ae80497f79f886278f1d47b1d4bf70963b941 # v2.0.2
    permissions:
      contents: read
      security-events: write
