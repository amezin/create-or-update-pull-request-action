name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '18 11 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@514700ca96e74c123ffb59d0bf3fc1e6b19b4793 # v1.1.2
    permissions:
      contents: read
      security-events: write
