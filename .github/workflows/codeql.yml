name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '18 11 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@f41ef584a55c4b52a294a78c8e48f35e015e4b1a # v2.0.0
    permissions:
      contents: read
      security-events: write
