name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '18 11 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@c282d8b68b27a0ebd4667d0168c30be058a4d640 # v1.0.5
    permissions:
      contents: read
      security-events: write
